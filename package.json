
@'
{
  "name": "MovieMint",
  "version": "1.0.0",
  "description": "MovieMint MERN project",
  "private": true,
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "install-deps": "npm install && cd client && npm install",
    "build": "cd client && npm run build"
  },
  "author": "Girish Shirke",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.6.3",
    "razorpay": "^2.9.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  },
  "engines": {
    "node": "18.x"
  }
}
'@ | Out-File -FilePath package.json -Encoding UTF8